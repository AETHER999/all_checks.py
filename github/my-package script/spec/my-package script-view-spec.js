'use babel';

import MyPackage scriptView from '../lib/my-package script-view';

describe('MyPackage scriptView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
